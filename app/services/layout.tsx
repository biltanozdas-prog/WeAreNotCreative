import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Services | WEARENOTCREATIVE",
  description: "Brand positioning, visual systems, art direction, identity architecture and product thinking. Structured creative services for design-led brands.",
}

export default function ServicesLayout({ children }: { children: React.ReactNode }) {
  return children
}
