import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Contact | WEARENOTCREATIVE",
  description: "Start a project with WEARENOTCREATIVE. For new projects, collaborations, and creative partnerships. Istanbul / Global.",
}

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children
}
